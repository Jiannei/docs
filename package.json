{
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^2.0.0-beta.26",
    "vuepress": "^2.0.0-beta.26"
  },
  "scripts": {
    "dev": "vuepress dev --config=config/config.ts --clean-cache",
    "build": "vuepress build --config=config/config.ts --clean-cache",
    "prod": "vuepress build --config=config/config.prod.ts --clean-cache",
    "deploy": "cloudbase framework deploy"
  }
}
